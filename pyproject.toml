[project]
name = "ekantipur-scraper"
version = "0.1.0"
description = "Scraper for ekantipur.com using Playwright"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.58.0",
]
